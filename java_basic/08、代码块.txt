根据位置和声明的不同：
将代码块分为：
	局部代码块：局部位置，用于限定变量的生命周期
	构造代码块：在类中的成员位置，有{}括起来，每次调用构造方法执行前，先会执行的构造代码块
		作用：可以把多个构造方法中的共同代码放到一起
	静态代码块：在类中的成员位置，只不过是用static 修饰，一般是对类进行初始化
		static{
			int age=10;
			String sex=boy;
			}

	执行顺序：同一个类中，静态代码块先于main方法执行，初始化类时先调用类静态代码块，构造方法调用时先调用构造代码块